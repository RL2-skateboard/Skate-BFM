# Training

This directory owns model-training code, training configuration, and data
preparation for the `train` branch.

Training datasets belong under `train/dataset/`. Generated checkpoints,
exports, and motion-library artifacts belong under `model/motion_library/`.
Large datasets and generated model files are local artifacts and are ignored
by Git.
